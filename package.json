{
  "name": "cv3",
  "version": "1.0.0",
  "description": "Matches a candidate's CV against 382 real scraped job postings using semantic embedding similarity, then uses Google Gemini (free tier) to explain the fit — strengths, gaps, and an overall recommendation — for the top matches. Entirely free/self-hosted, including the LLM.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
